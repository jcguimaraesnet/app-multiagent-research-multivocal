# Secrets — never commit real keys (Scopus, Firecrawl, OpenAI, Crossref)
.env
.env.*
!.env.example

# Pipeline outputs (each step writes data/<origin>/step-<n>.json)
data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.python-version

# OS / editor
.DS_Store
.vscode/
