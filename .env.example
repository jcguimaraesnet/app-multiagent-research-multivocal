# Copy this file to .env and fill in the values. Never commit the real .env.
FIRECRAWL_API_KEY=
SCOPUS_API_KEY=
OPENAI_API_KEY=
CROSSREF_EMAIL=

# OpenRouter (LLM screening, steps 2-4). OPENROUTER_MODEL is optional; it defaults to
# deepseek/deepseek-v4-pro. Change it to run a different model without touching the code.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
