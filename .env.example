# Copy this file to .env and fill in the values. Never commit the real .env.
FIRECRAWL_API_KEY=
SCOPUS_API_KEY=
# MODEL_EXPERIMENT is NOT sent to OpenRouter; it names the subfolder that isolates one
# model's screening outputs (steps 2-4): data/<origin>/<MODEL_EXPERIMENT>/. e.g. deepseek-v4-fast
MODEL_EXPERIMENT=
OPENROUTER_API_KEY=

